import { css } from '@emotion/react';

export const spoiler = css`
    position: relative;
    display: block;
    cursor: pointer;
    border: 1px solid #ccc;
    background-color: #000;
    opacity: 0.5;
`;
